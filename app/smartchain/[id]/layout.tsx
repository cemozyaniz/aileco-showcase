// The LanguageToggle is rendered in the root layout (app/layout.tsx),
// not in this page layout. No changes needed here.
export default function SmartChainLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
